extends layout

block content
    //- div(class="messageBoard")
    //-     h2(class="boardTitle") Welcome #{user.username}
    //-     div(class="createMessage")
    //-         form(id='newMessage', method="post", action="/Board")
    //-             textarea(type="text", name="message", class="messageText")
    //-             input(type="submit", value="Comment", class="messageSubmit")
    div(class="messageBoard")
        h2(class="boardTitle") Welcome #{user.username}
        +message
    each message in board
        div(class="message")
            p(class="messageDelete") Delete
            p(class="messageEdit") Edit
            p(class="messageUser") User: #{message.username}
            img(class="messageUser" src=message.imageURL) 
            p(class="messageContent") Message: #{message.message}
            p(class="messageDate") Date: #{message.date}