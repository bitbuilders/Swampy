doctype html

html
    head
        link(rel="stylesheet", href="./style.css")
    body
        nav
            ul
                each item in menu
                    li(class=item[2])
                        a(href=item[1]) #{item[0]}
        - var admin = user.isAdmin
        block content
        footer
            p &copy; Swampy Inc.